---
phase: 05-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/orchestrators/test_orchestrator_state_integration.py
  - tests/unit/crews/test_crew_output_parsing.py
autonomous: true

must_haves:
  truths:
    - "Orchestrator integration tests verify that FinwizState fields are actually mutated (not just return values checked) after calling check_portfolio, check_crypto, check_stock, check_etf, and analyze_and_update_portfolio"
    - "Concurrent deep analysis test verifies that results from 3+ holdings are collected correctly via asyncio.gather"
    - "Error propagation tests verify that failure in one orchestrator sets the correct *_error and *_success=False state fields"
    - "Crew output parsing tests exercise all 3 branches of the Pydantic access cascade (pydantic, json_dict, raw) for at least crypto, stock, and etf crews"
    - "Malformed crew output tests verify graceful degradation when result.pydantic.model_dump() raises AttributeError"
    - "Feature flag disabled tests verify that execute_*_crew returns the disabled dict without calling crew.kickoff()"
  artifacts:
    - path: "tests/unit/orchestrators/test_orchestrator_state_integration.py"
      provides: "Orchestrator integration tests with real FinwizState mutations"
      min_lines: 150
    - path: "tests/unit/crews/test_crew_output_parsing.py"
      provides: "CrewAI output format variation tests for crew_factory.py"
      min_lines: 120
  key_links:
    - from: "tests/unit/orchestrators/test_orchestrator_state_integration.py"
      to: "src/finwiz/flow_state.py"
      via: "imports FinwizState and asserts field mutations"
      pattern: "from finwiz\\.flow_state import FinwizState"
    - from: "tests/unit/orchestrators/test_orchestrator_state_integration.py"
      to: "src/finwiz/orchestrators/discovery_orchestrator.py"
      via: "tests DiscoveryOrchestrator state mutations"
      pattern: "DiscoveryOrchestrator"
    - from: "tests/unit/orchestrators/test_orchestrator_state_integration.py"
      to: "src/finwiz/orchestrators/deep_analysis_orchestrator.py"
      via: "tests DeepAnalysisOrchestrator state mutations"
      pattern: "DeepAnalysisOrchestrator"
    - from: "tests/unit/crews/test_crew_output_parsing.py"
      to: "src/finwiz/crew_factory.py"
      via: "tests the 3-branch output access cascade"
      pattern: "from finwiz\\.crew_factory import CrewFactory"
---

<objective>
Create orchestrator integration tests that verify real FinwizState mutations (TEST-01) and crew output parsing tests covering all CrewAI output format variations (TEST-02).

Purpose: These tests catch the most common class of bugs in FinWiz -- orchestrator methods that return correct values but silently fail to update shared state, and crew output parsing that only handles the happy path.

Output: Two new test files -- `test_orchestrator_state_integration.py` (orchestrator state mutation tests) and `test_crew_output_parsing.py` (crew output format variation tests).
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage/05-RESEARCH.md

Source files under test:
@src/finwiz/orchestrators/discovery_orchestrator.py
@src/finwiz/orchestrators/deep_analysis_orchestrator.py
@src/finwiz/orchestrators/reporting_orchestrator.py
@src/finwiz/orchestrators/error_handling_orchestrator.py
@src/finwiz/crew_factory.py
@src/finwiz/flow_state_models.py
@src/finwiz/flow_state.py

Reference test patterns:
@tests/unit/orchestrators/test_validation_orchestrator.py
@tests/fixtures/mock_factories.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrator state integration tests (TEST-01)</name>
  <files>
    tests/unit/orchestrators/test_orchestrator_state_integration.py
  </files>
  <action>
    Create `tests/unit/orchestrators/test_orchestrator_state_integration.py` containing a test class `TestOrchestratorStateIntegration` with the following tests. Follow the pattern from `test_validation_orchestrator.py` -- real `FinwizState()` instance, mocked dependencies via `mocker`.

    **CRITICAL RULES:**
    - Use `mocker` fixture (pytest-mock) exclusively. NEVER import from unittest.mock.
    - Every test must assert BOTH the return value AND the state field mutations.
    - Use `mocker.patch()` for module-level imports, `mocker.Mock()` for dependencies.
    - Mark async tests with `@pytest.mark.asyncio`.

    **Fixtures (inside the test class):**
    ```python
    @pytest.fixture
    def state(self):
        return FinwizState(
            session_id="test_integration",
            current_date="2025-11-17",
            portfolio_review={"holdings": [
                {"ticker": "AAPL", "asset_class": "stock", "name": "Apple Inc."},
                {"ticker": "SPY", "asset_class": "etf", "name": "SPDR S&P 500"},
            ]},
        )
    ```

    **Test 1: `test_discovery_check_crypto_sets_state_on_success`**
    - Create `DiscoveryOrchestrator(state)` with no extra dependencies.
    - Mock `finwiz.scoring.crypto_analyzer.analyze_crypto_opportunities` to return `{"analysis_summary": "3 opportunities found", "opportunities": [{"ticker": "BTC"}, {"ticker": "ETH"}], "performance_metrics": {"total": 2}}`.
    - Also mock `Path.mkdir` and `builtins.open` (via `mocker.patch`) to prevent real file I/O from `_save_discovery_results`.
    - Call `orchestrator.check_crypto()`.
    - Assert return dict has `crypto_analysis_complete: True`.
    - Assert `state.crypto_analysis_success is True`.
    - Assert `state.crypto_result` contains "3 opportunities found".
    - Assert `state.crypto_opportunities` is a list of length 2.

    **Test 2: `test_discovery_check_crypto_sets_state_on_failure`**
    - Mock `finwiz.scoring.crypto_analyzer.analyze_crypto_opportunities` to raise `RuntimeError("API down")`.
    - Mock file I/O as above.
    - Call `orchestrator.check_crypto()`.
    - Assert return dict has `crypto_analysis_complete: True` (always true, even on failure).
    - Assert `state.crypto_analysis_success is False`.
    - Assert `state.crypto_analysis_error` contains "API down".

    **Test 3: `test_discovery_check_stock_sets_state_on_success`**
    - Same pattern as crypto but mock `finwiz.scoring.stock_analyzer.analyze_stock_opportunities`.
    - Return `{"analysis_summary": "5 stocks found", "opportunities": [{"ticker": "NVDA"}], "performance_metrics": {}}`.
    - Assert `state.stock_analysis_success is True` and `state.stock_result` populated.

    **Test 4: `test_discovery_check_etf_sets_state_on_success`**
    - Same pattern, mock `finwiz.scoring.etf_analyzer.analyze_etf_opportunities`.
    - Assert `state.etf_analysis_success is True` and `state.etf_result` populated.

    **Test 5: `test_deep_analysis_sets_state_on_success`**
    - Create `DeepAnalysisOrchestrator(state, crew_factory=mocker.Mock(), integration_manager=mocker.Mock(), error_handler=mocker.Mock())`.
    - Mock `os.getenv` for `DEEP_PORTFOLIO_ANALYSIS` to return `"true"`.
    - Mock `orchestrator.run_deep_analysis_concurrent` as `mocker.AsyncMock` returning `{"AAPL": mock_deep_result}` where `mock_deep_result` is a `DeepAnalysisResult(ticker="AAPL", asset_class="stock", crew_name="test", grade="A", composite_score=0.82, recommendation="BUY", rationale="Strong", data_freshness_hours=0.5, confidence_level=0.85, risk_score=2.0)`.
    - Mock `orchestrator._match_alternatives` to return `{}`.
    - Mock `orchestrator._update_portfolio_review_with_enriched_data` to do nothing.
    - Call `await orchestrator.analyze_and_update_portfolio()`.
    - Assert `state.deep_analysis_results` is a dict with key "AAPL".
    - Assert `state.deep_analysis_success is True`.

    **Test 6: `test_deep_analysis_sets_error_state_on_failure`**
    - Same setup but mock `run_deep_analysis_concurrent` to raise `RuntimeError("Analysis timeout")`.
    - Call `await orchestrator.analyze_and_update_portfolio()`.
    - Assert `state.deep_analysis_success is False`.
    - Assert `state.deep_analysis_error` contains "Analysis timeout".

    **Test 7: `test_discovery_consolidation_aggregates_all_opportunities`**
    - Create `DiscoveryOrchestrator(state)`.
    - Set `state.crypto_opportunities = [{"ticker": "BTC"}]`, `state.stock_opportunities = [{"ticker": "NVDA"}, {"ticker": "TSLA"}]`, `state.etf_opportunities = [{"ticker": "QQQ"}]`.
    - Mock file I/O as above.
    - Call `orchestrator.check_investment_discovery()`.
    - Assert `state.investment_discovery_available is True`.
    - Assert `state.all_discovery_opportunities` has length 4 (1+2+1).

    **Test 8: `test_error_propagation_across_orchestrator_boundaries`**
    - Create `DiscoveryOrchestrator(state)`.
    - Mock `analyze_crypto_opportunities` to raise `ValueError("bad data")`.
    - Mock file I/O.
    - Call `orchestrator.check_crypto()`.
    - Assert `state.crypto_analysis_success is False`.
    - Now create a SEPARATE orchestrator (e.g., `DiscoveryOrchestrator(state)`) sharing the SAME state.
    - Call `orchestrator.check_investment_discovery()`.
    - Verify the consolidation sees `state.crypto_opportunities` as empty/None (because the error path did not set it) but does NOT crash.

    Keep the file under 300 lines. Each test should be 15-25 lines. Use descriptive `test_should_*` or `test_*_sets_*` naming.
  </action>
  <verify>
    Run: `uv run pytest tests/unit/orchestrators/test_orchestrator_state_integration.py -v --tb=short`
    All 8 tests pass. No import errors. No warnings about unittest.mock.
  </verify>
  <done>
    8 orchestrator integration tests pass, covering: DiscoveryOrchestrator (crypto/stock/etf success + failure + consolidation), DeepAnalysisOrchestrator (success + failure), and error propagation across orchestrator boundaries. Each test verifies BOTH return values AND FinwizState field mutations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crew output parsing tests (TEST-02)</name>
  <files>
    tests/unit/crews/test_crew_output_parsing.py
  </files>
  <action>
    Create `tests/unit/crews/test_crew_output_parsing.py` containing a test class `TestCrewOutputParsing`. This tests the 3-branch Pydantic access cascade in `crew_factory.py` (lines 64-70, 119-125, 174-180).

    **CRITICAL RULES:**
    - Use `mocker` fixture exclusively. NEVER import from unittest.mock.
    - Reuse `mock_crew_result` fixture from `tests/fixtures/mock_factories.py` for creating mock CrewAI results.
    - Mock crew class instantiation and `crew().kickoff()` chain.
    - Mock `is_feature_enabled` to return True for each crew type.

    **Fixtures (inside the test class):**
    ```python
    @pytest.fixture
    def factory(self, mocker):
        integration_manager = mocker.Mock()
        error_handler = mocker.Mock()
        return CrewFactory(
            integration_manager=integration_manager,
            error_handler=error_handler,
        )
    ```

    **Create a helper fixture for mocking crew kickoff:**
    ```python
    @pytest.fixture
    def mock_crew_kickoff(self, mocker):
        """Helper to mock a crew class so crew().kickoff(inputs=...) returns a given result."""
        def _setup(crew_module_path, mock_result):
            mock_crew_class = mocker.patch(crew_module_path)
            mock_crew_instance = mock_crew_class.return_value
            mock_crew_obj = mock_crew_instance.crew.return_value
            mock_crew_obj.kickoff.return_value = mock_result
            return mock_crew_obj
        return _setup
    ```

    **Tests for crypto crew (same pattern repeated for stock and etf):**

    **Test 1: `test_crypto_crew_pydantic_output`**
    - Mock `is_feature_enabled` to return True.
    - Create a mock pydantic model: `mock_pydantic = mocker.Mock(); mock_pydantic.model_dump.return_value = {"recommendation": "BUY", "score": 0.85}`.
    - Create mock result with `pydantic=mock_pydantic, json_dict=None, raw="fallback text"`.
    - Use `mock_crew_kickoff("finwiz.crew_factory.CryptoCrew", mock_result)`.
    - Call `factory.execute_crypto_crew({"ticker": "BTC"})`.
    - Assert `result["crypto_analysis_success"] is True`.
    - Assert `result["crypto_analysis_result"] == {"recommendation": "BUY", "score": 0.85}`.

    **Test 2: `test_crypto_crew_json_dict_output`**
    - Create mock result with `pydantic=None, json_dict={"recommendation": "HOLD"}, raw="fallback"`.
    - Assert `result["crypto_analysis_result"] == {"recommendation": "HOLD"}`.

    **Test 3: `test_crypto_crew_raw_fallback_output`**
    - Create mock result with `pydantic=None, json_dict=None` (note: mock_crew_result sets `json_dict={}` by default which is falsy).
    - Ensure json_dict evaluates to falsy: set `mock_result.json_dict = None` after creation, or use `mocker.Mock()` directly with `pydantic=None, json_dict=None, raw="Raw analysis text"`.
    - Assert `result["crypto_analysis_result"] == {"raw_output": "Raw analysis text"}`.

    **Test 4: `test_crypto_crew_pydantic_model_dump_error`**
    - Create mock result where `pydantic` is truthy but `model_dump()` raises `AttributeError`.
    - This should trigger the exception path (line 75 of crew_factory.py).
    - Assert `result["crypto_analysis_success"] is False` (error handled by outer try/except).
    - Assert `result["crypto_analysis_error"]` is a string containing the error.

    **Test 5: `test_stock_crew_pydantic_output`**
    - Same as Test 1 but for `execute_stock_crew` with `mock_crew_kickoff("finwiz.crew_factory.StockCrew", ...)`.
    - Assert `result["stock_analysis_result"]` matches the pydantic model_dump.

    **Test 6: `test_stock_crew_json_dict_output`**
    - json_dict path for stock crew.

    **Test 7: `test_stock_crew_raw_fallback_output`**
    - raw fallback path for stock crew.

    **Test 8: `test_etf_crew_pydantic_output`**
    - Same pattern for `execute_etf_crew` with `mock_crew_kickoff("finwiz.crew_factory.EtfCrew", ...)`.

    **Test 9: `test_etf_crew_raw_fallback_output`**
    - raw fallback path for etf crew.

    **Test 10: `test_crypto_crew_feature_flag_disabled`**
    - Mock `is_feature_enabled` to return False for "crypto_analysis".
    - Call `factory.execute_crypto_crew({"ticker": "BTC"})`.
    - Assert result is `{"crypto_analysis_disabled": True}`.
    - Assert the crew class was NOT instantiated (no kickoff call).

    **Test 11: `test_stock_crew_execution_failure_uses_error_handler`**
    - Mock `StockCrew().crew().kickoff()` to raise `RuntimeError("API rate limit")`.
    - Mock `error_handler.handle_crew_failure` to return a mock with `success=True, data={"cached": True}, fallback_strategy="cache", degraded_functionality=["no_live_data"], message="Using cached data"`.
    - Assert `result["stock_analysis_success"] is False`.
    - Assert `result["stock_analysis_fallback"] is True`.
    - Assert `result["stock_analysis_result"] == {"cached": True}`.

    Keep the file under 300 lines. Use `@pytest.mark.parametrize` if it helps reduce repetition across crypto/stock/etf, but only if it keeps tests readable.
  </action>
  <verify>
    Run: `uv run pytest tests/unit/crews/test_crew_output_parsing.py -v --tb=short`
    All 11 tests pass. No import errors. No warnings about unittest.mock.
    Also run: `uv run pytest tests/unit/crews/ tests/unit/orchestrators/test_orchestrator_state_integration.py -v --tb=short` to ensure no conflicts.
  </verify>
  <done>
    11 crew output parsing tests pass, covering: all 3 branches of the Pydantic access cascade (pydantic, json_dict, raw) for crypto, stock, and etf crews; pydantic model_dump error handling; feature flag disabled path; and crew execution failure with error handler fallback.
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
uv run pytest tests/unit/orchestrators/test_orchestrator_state_integration.py tests/unit/crews/test_crew_output_parsing.py -v --tb=short
```
Expected: 19 tests pass (8 + 11), 0 failures, 0 errors.

Then run broader checks:
```bash
make check
```
Expected: lint passes, existing tests still pass, no unittest.mock violations detected.
</verification>

<success_criteria>
1. 8 orchestrator state integration tests pass -- each verifies return value AND state mutation
2. 11 crew output parsing tests pass -- all 3 cascade branches covered for crypto/stock/etf
3. No unittest.mock imports in any new test file
4. No new test failures in existing tests (`make test` clean)
5. Both new files are under 300 lines each
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage/05-01-SUMMARY.md`
</output>
